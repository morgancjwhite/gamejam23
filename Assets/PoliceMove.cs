using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PoliceMove : Human
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    private 

    // Update is called once per frame
    void Update()
    {
        
    }
}
